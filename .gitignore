# Environments / secrets
.env
.venv/
venv/

# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/

# Model caches (HuggingFace, torch hub) — never commit weights
.hf-cache/
models/
*.pt
*.onnx
*.safetensors

# Tooling
.pytest_cache/
.idea/
.vscode/
.claude/settings.local.json

# OS
.DS_Store
Thumbs.db

# M3 annotation logs — local data, never committed
data/*
!data/.gitkeep
# Curated playlist mapping: the committed copy is the founder's baseline;
# users may edit locally without committing to override it.
!data/playlists.json
