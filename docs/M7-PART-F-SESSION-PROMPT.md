# M7 part (f): session kickoff prompt

**Purpose.** Written 2026-09-24. This is the prompt to paste into Claude Code on
JPad once 4–6 people are in the room for M7 part (f). It's self-contained, so a
fresh session starts correctly. It wraps `docs/M7-PART-F-RUN-SHEET.md` and
carries over the 2026-09-24 FIELD-NOTES lessons: record the volume, keep a
ground-truth log, and wait after pausing before stopping the dashboard (the
anchor finding, ROADMAP M11-02). Replace `[N]` with the head count before
sending.

```
I have [N] people in the room and want to run M7 part (f) tonight on JPad,
following docs/M7-PART-F-RUN-SHEET.md.

1. Run the section B preflight. Stop and tell me if anything fails; don't
   work around it.
   - Checking out milestone-7-stable-middle may conflict with my
     uncommitted data/playlists.json edit. Don't discard or overwrite it;
     ask me first.
   - Confirm JPAD is visible in Spotify, the real playlist mapping resolves,
     and rescue is off.
2. Give me the exact commands for starting the dashboard myself. I start
   the mic session; you verify the launch and follow along.
3. Carry over from the 2026-09-24 field notes:
   - Record Windows volume (75 %) and Spotify volume (100 %) at the start.
   - Keep a ground-truth log: I'll tell you arrivals and departures as they
     happen, so note each against the wall clock.
   - When the session ends, remind me to wait about a minute after pausing
     before I stop the dashboard (M11-02, the anchor finding).
4. Afterwards, check the four part (f) checkpoints against the evidence and
   draft the FIELD-NOTES entry for me to review. Don't commit, merge or push
   anything without asking.
```
